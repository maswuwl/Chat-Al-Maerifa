
# 🚀 دليل النشر على GitHub و Firebase

## 1. الرفع على GitHub
1. قم بإنشاء مستودع (Repository) جديد.
2. ارفع الأكواد باستخدام:
   ```bash
   git init
   git add .
   git commit -m "Initial Studio Deploy"
   git remote add origin <URL>
   git push -u origin main
   ```

## 2. تفعيل Firebase
1. اذهب إلى [Firebase Console](https://console.firebase.google.com/).
2. أنشئ مشروعاً جديداً.
3. قم بتفعيل **Firestore Database** و **Hosting**.
4. انسخ بيانات الـ Config وضعها في ملف `firebase.ts`.

## 3. الربط التلقائي (GitHub Actions)
المشروع مهيأ ليعمل مع `base: './'` لضمان عمل كافة الأدوات الهجينة (مثل البورصة واستوديو تانجو) بعد النشر مباشرة.

**ملاحظة:** تأكد من إضافة `API_KEY` في قسم **Secrets** في GitHub لضمان عمل الذكاء الاصطناعي.
